import Foundation

final class Sync{{entity}}UseCase {
private let service = {{entity}}SyncService()

func execute(with deltas: [{{entity}}DeltaDto]) async throws {
let request = Sync{{entity}}Request(deltas: deltas)
try await service.sync(request: request)
}
}
